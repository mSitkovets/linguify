<tr>
  <td><%= line_item.quiz.title %></td>
  <td class="language"><%= line_item.quiz.language_learning %></td>
  <td><%= link_to 'Remove', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td> 
</tr>